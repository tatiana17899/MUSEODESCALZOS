@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a  class="nav-link text-white" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity?.Name!</a>
    </li>

}
else
{
    <li class="nav-item">
        <button class="nav-link text-white btn btn-log mx-2" asp-area="Identity" asp-page="/Account/Register" id="register">Registrar</button>
    </li>
    <li class="nav-item">
        <button class="nav-link btn btn-log mx-2" asp-area="Identity" asp-page="/Account/Login" id="login">Iniciar Sesion</button>
    </li>
}
</ul>
